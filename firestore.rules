rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow anyone to read artworks
    match /artworks/{document} {
      allow read: if true;
      allow write: if request.auth != null;  // Only logged-in users (like admin) can write
    }
  }
}
